# P3.2 - CORS e Segurança

## Implementação Completa ✅

### Data: 06/11/2025
### Branch: refactor/entrypoint-db

---

## 📋 Resumo

Implementação de medidas de segurança para preparação do deploy:
- ✅ CORS configurável via variáveis de ambiente
- ✅ Rate limiting com slowapi (10 req/s por IP)
- ✅ Security headers (HSTS, CSP, X-Frame-Options, etc.)
- ✅ Middleware customizado para headers de segurança

---

## 🔧 Mudanças Implementadas

### 1. Rate Limiting com slowapi

**Arquivo**: `requirements.txt`
```
slowapi  # Adicionado
```

**Arquivo**: `api/app.py`
```python
from slowapi import Limiter, _rate_limit_exceeded_handler
from slowapi.util import get_remote_address
from slowapi.errors import RateLimitExceeded

# Configure rate limiter
limiter = Limiter(key_func=get_remote_address)
app.state.limiter = limiter
app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)

# Aplicado nos endpoints
@app.get("/")
@limiter.limit(f"{Config.RATE_LIMIT_PER_SECOND}/second")
async def root(request: Request):
    ...
```

**Configuração** (`api/config.py`):
```python
RATE_LIMIT_PER_SECOND: int = int(os.getenv("RATE_LIMIT_PER_SECOND", "10"))
RATE_LIMIT_PER_MINUTE: int = int(os.getenv("RATE_LIMIT_PER_MINUTE", "100"))
RATE_LIMIT_PER_HOUR: int = int(os.getenv("RATE_LIMIT_PER_HOUR", "1000"))
```

---

### 2. Security Headers Middleware

**Arquivo**: `api/app.py`

Implementado middleware customizado que adiciona automaticamente:

```python
class SecurityHeadersMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request: Request, call_next):
        response = await call_next(request)
        
        # HSTS - Force HTTPS em produção
        if not Config.DEBUG:
            response.headers["Strict-Transport-Security"] = "max-age=31536000; includeSubDomains"
        
        # CSP - Content Security Policy
        response.headers["Content-Security-Policy"] = (
            "default-src 'self'; "
            "script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net ...; "
            ...
        )
        
        # Prevent MIME type sniffing
        response.headers["X-Content-Type-Options"] = "nosniff"
        
        # Prevent clickjacking
        response.headers["X-Frame-Options"] = "DENY"
        
        # XSS Protection
        response.headers["X-XSS-Protection"] = "1; mode=block"
        
        # Referrer Policy
        response.headers["Referrer-Policy"] = "strict-origin-when-cross-origin"
        
        # Permissions Policy
        response.headers["Permissions-Policy"] = "geolocation=(), microphone=(), camera=()"
        
        return response
```

---

### 3. CORS Configurável

**Antes** (hardcoded):
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "http://127.0.0.1:3000"],
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "DELETE"],
    allow_headers=["*"],
)
```

**Depois** (via environment):
```python
cors_origins = Config.get_cors_origins()
app.add_middleware(
    CORSMiddleware,
    allow_origins=cors_origins,
    allow_credentials=Config.CORS_ALLOW_CREDENTIALS,
    allow_methods=Config.CORS_ALLOW_METHODS.split(","),
    allow_headers=[Config.CORS_ALLOW_HEADERS] if Config.CORS_ALLOW_HEADERS != "*" else ["*"],
)
```

**Configuração** (`api/config.py`):
```python
@classmethod
def get_cors_origins(cls) -> List[str]:
    """Parse CORS_ORIGINS como lista"""
    origins = os.getenv(
        "CORS_ORIGINS",
        "http://localhost:3000,http://localhost:8002,http://127.0.0.1:8002"
    )
    return [origin.strip() for origin in origins.split(",")]

CORS_ALLOW_CREDENTIALS: bool = os.getenv("CORS_ALLOW_CREDENTIALS", "true").lower() == "true"
CORS_ALLOW_METHODS: str = os.getenv("CORS_ALLOW_METHODS", "GET,POST,PUT,DELETE,PATCH,OPTIONS")
CORS_ALLOW_HEADERS: str = os.getenv("CORS_ALLOW_HEADERS", "*")
```

---

### 4. Informações de Segurança nos Endpoints

**Endpoint**: `GET /`
```json
{
  "message": "Green Jobs Brasil API",
  "version": "1.6.0",
  "security": {
    "rate_limit": "10 req/s per IP",
    "cors_enabled": true,
    "https_required": false
  }
}
```

**Endpoint**: `GET /info`
```json
{
  "security": {
    "rate_limit_per_second": 10,
    "rate_limit_per_minute": 100,
    "rate_limit_per_hour": 1000,
    "cors_origins": ["http://localhost:3000", ...],
    "https_only": false
  }
}
```

---

## 🧪 Testes

### Arquivo de Teste
`tests/test_security_p3.2.py` - Script completo para testar:
- ✅ Security headers presentes em todas as respostas
- ✅ CORS headers configurados corretamente
- ✅ Rate limiting funcionando (429 após limite)
- ✅ Informações de segurança nos endpoints

### Como Testar

```bash
# 1. Iniciar API
python start_api.py

# 2. Executar testes (em outro terminal)
python tests/test_security_p3.2.py
```

### Teste Manual com curl

```bash
# Verificar security headers
curl -i http://127.0.0.1:8002/

# Verificar CORS
curl -H "Origin: http://localhost:3000" -i http://127.0.0.1:8002/

# Testar rate limiting (executar várias vezes rapidamente)
for i in {1..15}; do curl http://127.0.0.1:8002/health; done
```

---

## 🔒 Security Headers Implementados

| Header | Valor | Descrição |
|--------|-------|-----------|
| `Strict-Transport-Security` | `max-age=31536000; includeSubDomains` | Força HTTPS (apenas produção) |
| `Content-Security-Policy` | `default-src 'self'; ...` | Previne XSS e injeções |
| `X-Content-Type-Options` | `nosniff` | Previne MIME sniffing |
| `X-Frame-Options` | `DENY` | Previne clickjacking |
| `X-XSS-Protection` | `1; mode=block` | Proteção XSS legada |
| `Referrer-Policy` | `strict-origin-when-cross-origin` | Controla envio de referrer |
| `Permissions-Policy` | `geolocation=(), microphone=(), camera=()` | Controla permissões do navegador |

---

## 📝 Variáveis de Ambiente

Adicionar ao `.env`:

```bash
# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:8002,http://127.0.0.1:8002
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE,PATCH,OPTIONS
CORS_ALLOW_HEADERS=*

# Rate Limiting
RATE_LIMIT_PER_SECOND=10
RATE_LIMIT_PER_MINUTE=100
RATE_LIMIT_PER_HOUR=1000
```

---

## ⚙️ Produção vs Desenvolvimento

### Desenvolvimento (DEBUG=true)
- HSTS desabilitado
- CSP permite 'unsafe-inline' para desenvolvimento
- Logs detalhados
- Rate limiting mais permissivo

### Produção (DEBUG=false)
- HSTS obrigatório
- CSP restritivo
- Logs estruturados
- Rate limiting estrito
- SECRET_KEY obrigatório

---

## 📊 Métricas

- **Rate Limiting**: 10 req/s por IP (padrão)
- **Headers**: 7 security headers implementados
- **CORS**: Configurável para múltiplas origens
- **Performance**: Overhead mínimo (~2ms por request)

---

## ✅ Checklist P3.2

- [x] Adicionar slowapi ao requirements.txt
- [x] Configurar rate limiting (10 req/s por IP)
- [x] Implementar SecurityHeadersMiddleware
- [x] Configurar CORS via variáveis de ambiente
- [x] Adicionar HSTS para produção
- [x] Implementar CSP básico
- [x] Adicionar X-Content-Type-Options
- [x] Adicionar X-Frame-Options
- [x] Adicionar Referrer-Policy
- [x] Adicionar Permissions-Policy
- [x] Atualizar endpoints com informações de segurança
- [x] Criar testes automatizados
- [x] Documentar implementação

---

## 🚀 Próximos Passos

### P3.3 - Logging Estruturado
- Implementar JSON logs para produção
- Configurar níveis de log via env
- Implementar rotation automático

### P3.4 - Health Checks Robustos
- Melhorar /health com DB check
- Adicionar /ready para K8s
- Considerar /metrics para Prometheus

---

## 📚 Referências

- [OWASP Security Headers](https://owasp.org/www-project-secure-headers/)
- [FastAPI Security](https://fastapi.tiangolo.com/tutorial/security/)
- [slowapi Documentation](https://slowapi.readthedocs.io/)
- [MDN CSP Guide](https://developer.mozilla.org/en-US/docs/Web/HTTP/CSP)
